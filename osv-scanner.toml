# osv-scanner accepted-risk allowlist.
#
# Each entry must include reasoning + a re-evaluation date. Re-run
# `osv-scanner --lockfile=bun.lock` periodically to confirm each remains
# accepted-risk. bun.lock is canonical.
#
# Currently EMPTY — nothing is accepted-risk. Reviewed 2026-09-25: every entry
# that was here (esbuild, dompurify, undici via miniflare, valibot) had expired
# (ignoreUntil 2026-09-12) and no longer matched anything in bun.lock. The
# advisories open at review time were all fixable and are pinned to patched
# versions in the root package.json `overrides` instead (devalue, js-yaml,
# lodash-es, smol-toml, svgo, valibot) — prefer that over allowlisting.
